# 电影购票系统（仿猫眼电影）—— 项目汇报文档

> **课程**：软件工程实践
> **项目名称**：影院管理系统（Cinema Booking System）
> **技术栈**：Spring Boot 3 + Vue 3 + TypeScript + MySQL

---

## 一、项目概述

本项目是一个**前后端完全分离**的在线电影购票系统，仿照猫眼电影的核心功能进行设计开发。系统支持三种用户角色，实现了从电影浏览、在线选座购票到后台管理的完整业务闭环。

### 核心亮点

- **三角色权限体系**：超级管理员、影院管理员、普通用户，各司其职
- **在线选座购票**：8×8 座位图实时选座，支持锁座与超时自动释放
- **排期冲突检测**：同一影厅同一时间段不可重复排期
- **审核工作流**：影院入驻审核、场次放映审批
- **数据可视化**：ECharts 图表展示票房趋势、类型分布等统计数据
- **预告片播放**：电影详情页集成视频预告片播放功能
- **真实电影海报**：通过 TMDB API 获取真实海报 URL，带默认图 fallback 机制
- **用户注册与账户管理**：支持用户自主注册、注销账户

---

## 二、技术架构

### 2.1 整体架构

```
┌─────────────┐     HTTP/REST API     ┌──────────────────┐     MyBatis     ┌──────────┐
│   Vue 3 前端  │ ◄──────────────────► │  Spring Boot 后端  │ ◄────────────► │  MySQL   │
│  (Vite 构建)  │    JSON + JWT Token   │   (Maven 构建)     │                │  数据库   │
└─────────────┘                        └──────────────────┘                └──────────┘
```

### 2.2 技术选型

| 层级 | 技术 | 版本 | 选型理由 |
|------|------|------|----------|
| **后端框架** | Spring Boot | 3.3.5 | Java 生态主流框架，快速开发 REST API |
| **ORM** | MyBatis | 3.0.3 | 灵活的 SQL 映射，适合复杂查询场景 |
| **前端框架** | Vue 3 | 3.4+ | 组合式 API，性能优秀，学习曲线平缓 |
| **前端语言** | TypeScript | 5.3+ | 类型安全，提升代码可维护性 |
| **UI 组件库** | Element Plus | 2.7+ | Vue 3 生态最成熟的 UI 组件库 |
| **CSS 框架** | Tailwind CSS | 3.4+ | 原子化 CSS，快速构建界面 |
| **状态管理** | Pinia | 2.1+ | Vue 3 官方推荐的状态管理方案 |
| **图表库** | ECharts | 5.5+ | 功能强大的数据可视化库 |
| **数据库** | MySQL | 8.0 | 成熟稳定的关系型数据库 |
| **认证方案** | JWT  | — | 无状态认证，适合前后端分离架构 |
| **构建工具** | Vite / Maven | — | 前后端各自的标准化构建工具 |

### 2.3 开发环境要求

- JDK >= 17
- Node.js >= 18
- Maven >= 3.8
- MySQL 5.7 / 8.0

---

## 三、系统设计

### 3.1 角色权限设计

系统设计了三种角色，形成清晰的权限层级：

```
┌──────────────────────────────────────────────────────┐
│                  超级管理员 (ADMIN)                      │
│    审核影院 · 管理电影 · 数据统计 · 公告管理 · 账号管理      │
├──────────────────────────────────────────────────────┤
│                影院管理员 (CINEMA_ADMIN)                 │
│       影厅管理 · 场次排片 · 查看本影院订单                   │
├──────────────────────────────────────────────────────┤
│                  普通用户 (USER)                         │
│       浏览电影 · 选座购票 · 收藏评分 · 查看订单              │
└──────────────────────────────────────────────────────┘
```

**权限实现方式**：
- 后端：自定义 `@RequireRole` 注解 + `AuthInterceptor` 拦截器，实现声明式权限控制
- 前端：Vue Router 导航守卫 + 路由 `meta.roles` 配置，实现页面级访问控制
- 未登录用户可浏览电影首页和详情，受限操作（购票、评论）引导登录

### 3.2 数据库设计

数据库共 **14 张表**，采用 `t_` 前缀命名规范，`utf8mb4_unicode_ci` 字符集。

**ER 关系概览**：

```
t_admin (超级管理员)
    │
    ├── 审核影院管理员 ──► t_cinema_admin ──► t_cinema (影院)
    │                                          │
    │                                          ├── t_hall (影厅)
    │                                          │       │
    │                                          │       └── t_showing (场次) ──► t_movie (电影)
    │                                          │              │                    │
    │                                          │              │                    ├── t_movie_type_rel ──► t_movie_type
    │                                          │              │                    └── t_movie_staff (演职人员)
    │                                          │              │
    │                                          │              └── t_order (订单) ──► t_user
    │                                          │                     │
    │                                          │                     └── t_order_seat (座位明细)
    │                                          │
    └── 发布公告 ──► t_notice

t_user (普通用户)
    │
    └── t_movie_user_action (收藏/评分) ──► t_movie
```

**核心表说明**：

| 表名 | 用途 | 关键设计 |
|------|------|----------|
| `t_admin` | 超级管理员账号 | 独立表，与普通用户分离 |
| `t_user` | 普通用户账号 | 含手机号字段，支持自主注册注销 |
| `t_cinema` | 影院信息 | 含封面、标签、地址等 |
| `t_cinema_admin` | 影院管理员 | 含审核状态、关联影院 ID |
| `t_hall` | 影厅 | 默认 8×8 座位布局 |
| `t_movie` | 电影信息 | 冗余缓存票房和评分字段，真实海报 URL |
| `t_movie_type` / `t_movie_type_rel` | 电影类型及关联 | 多对多关系，每部电影 1-3 个类型 |
| `t_movie_staff` | 演职人员 | role_type 区分导演/编剧/主演 |
| `t_showing` | 放映场次 | 含审核状态、唯一约束防冲突 |
| `t_order` | 购票订单 | 含锁座超时机制 |
| `t_order_seat` | 订单座位明细 | 记录每个订单的具体座位 |
| `t_notice` | 系统公告 | 含发布状态管理 |
| `t_movie_user_action` | 用户收藏/评分 | 合并收藏与评分为一张表 |

### 3.3 核心业务流程

#### 流程一：选座购票（核心流程）

```
用户浏览电影 → 选择场次 → 查看座位图 → 选择座位 → 提交订单
                                                    │
                                            ┌───────┴───────┐
                                            │  订单创建成功    │
                                            │  状态: 待支付    │
                                            │  座位: 已锁定    │
                                            │  超时: 10分钟    │
                                            └───────┬───────┘
                                                    │
                                        ┌───────────┼───────────┐
                                        ▼                       ▼
                                  用户支付                  超时未支付
                                状态→待取票              定时任务取消
                                        │                  状态→已超时取消
                                        ▼
                                  用户取票
                                状态→已完成
```

**技术要点**：
- 下单时锁定座位，设置 `lock_expire_time` 为当前时间 + 10 分钟
- 后端定时任务自动扫描超时订单并释放座位
- 订单表冗余存储 `cinema_id`、`hall_id`、`movie_id`，避免多表关联查询

#### 流程二：排期管理（影院管理员）

```
影院管理员登录 → 进入排片管理 → 新增/编辑排期
                                        │
                                        ▼
                                冲突检测（同影厅同时间段）
                                        │
                              ┌─────────┴─────────┐
                              ▼                   ▼
                            无冲突              有冲突
                              │                   │
                              ▼                   ▼
                          保存排期            提示错误
                              │
                              ▼
                        等待超级管理员审核
                              │
                              ▼
                          审核通过后售票
```

**技术要点**：
- 排期保存前检查影厅时间段是否重叠
- 排期需超级管理员审核通过后才可售票
- 支持排期的编辑和删除操作

#### 流程三：审核工作流

```
影院管理员注册 ──► 提交影院信息 ──► 等待审核 ──► 超级管理员审批
                                                    │
                                            ┌───────┴───────┐
                                            ▼               ▼
                                          审核通过          审核驳回
                                        绑定影院ID        返回驳回原因
                                        可正常使用

影院管理员提交场次 ──► 等待审核 ──► 超级管理员审批 ──► 通过后可售票
```

---

## 四、功能模块详解

### 4.1 前端页面结构（共 19 个页面）

#### 公共页面
- **登录页**：统一入口，支持三种角色登录
- **注册页**：用户自主注册，自动审核通过
- **首页**：系统落地页，电影推荐

#### 普通用户端（5 个页面，路径 `/u`）
| 页面 | 功能 |
|------|------|
| 用户首页 | 热播/待映推荐、Top10 排行、今日票房 |
| 电影列表 | 按类型/年代/地区筛选，支持搜索 |
| 电影详情 | 海报、简介、预告片、评分、评论 |
| 选座购票 | 8×8 座位图选座、下单、支付 |
| 我的订单 | 按订单号查询，取消待支付订单 |

#### 影院管理员端（5 个页面，路径 `/staff`）
| 页面 | 功能 |
|------|------|
| 员工首页 | 工作台概览 |
| 影院管理 | 影院基本信息维护 |
| 影厅管理 | 影厅增删改、座位布局设置 |
| 排片管理 | 提交放映场次申请，支持冲突检测 |
| 订单管理 | 查看本影院订单 |

#### 超级管理员端（8 个页面，路径 `/admin`）
| 页面 | 功能 |
|------|------|
| 管理首页 | 后台概览 |
| 数据统计 | 近 7 日票房折线图、类型饼图、票房柱图 |
| 影院审核 | 审核影院管理员入驻申请 |
| 电影管理 | 电影信息 CRUD、类型管理、演职人员管理 |
| 排片管理 | 全局场次审核（通过/驳回/下架） |
| 订单管理 | 全部订单查看与管理 |
| 公告管理 | 系统公告发布与管理 |
| 账号管理 | 用户/管理员/影院管理员账号管理 |

### 4.2 后端 API 设计

后端采用 RESTful 风格设计 API，统一响应格式：

```json
{
  "code": 0,
  "message": "success",
  "data": { ... }
}
```

**主要 API 模块**：

| 模块 | 路径前缀 | 说明 |
|------|----------|------|
| 认证 | `/api/auth` | 登录、用户注册、影院管理员注册 |
| 电影（公开） | `/api/movies` | 电影列表、详情、搜索 |
| 场次（公开） | `/api/showings` | 场次查询、座位图 |
| 用户订单 | `/api/user/orders` | 创建、支付、取消、查询订单 |
| 影院管理 | `/api/cinema-admin/*` | 影院管理员业务 |
| 管理后台 | `/api/admin/*` | 超级管理员全部功能 |
| 数据统计 | `/api/stats` | 统计数据接口 |

---

## 五、项目结构

```
cinemabookingsystem/
├── cinema-backend/                    # 后端工程
│   └── src/main/java/com/xd/cinema/
│       ├── CinemaBackendApplication   # 启动类
│       ├── auth/                      # 认证模块（登录/注册）
│       ├── common/                    # 公共模块
│       │   ├── api/                   # 统一响应封装 (ApiResult, PageResult)
│       │   ├── enums/                 # 枚举类（角色、状态等）
│       │   ├── exception/             # 全局异常处理
│       │   ├── security/              # JWT + 拦截器 + 权限注解
│       │   └── util/                  # 工具类
│       ├── config/                    # 配置类（CORS、拦截器注册）
│       └── module/                    # 业务模块（按领域划分）
│           ├── action/                # 用户行为（收藏/评分）
│           ├── admin/                 # 超级管理员功能
│           ├── cinema/                # 影院管理
│           ├── home/                  # 首页数据
│           ├── movie/                 # 电影管理
│           ├── notice/                # 系统公告
│           ├── order/                 # 订单管理（含定时任务）
│           ├── profile/               # 用户个人中心
│           ├── showing/               # 放映场次
│           ├── stats/                 # 数据统计
│           └── user/                  # 用户基础
│
├── cinema-frontend/                   # 前端工程
│   └── src/
│       ├── api/                       # API 接口封装（Axios）
│       ├── components/                # 公共组件
│       ├── composables/               # 组合式函数
│       ├── layouts/                   # 布局组件（三角色共用）
│       ├── pages/                     # 页面视图（19个页面）
│       │   ├── admin/                 # 管理员页面（8个）
│       │   ├── staff/                 # 影院员工页面（5个）
│       │   └── user/                  # 用户页面（5个）
│       ├── router/                    # 路由配置 + 导航守卫
│       └── stores/                    # Pinia 状态管理
│
├── database.sql                       # 数据库脚本（14张表 + 初始数据）
├── vercel.json                        # Vercel 部署配置
├── README.md                          # 项目快速启动指南
├── 项目汇报文档.md                     # 详细技术汇报文档（本文档）
└── 新手解读文档.md                     # 零基础入门解读
```

---

## 六、关键技术实现

### 6.1 JWT 认证机制

- 用户登录后，后端生成 JWT Token 返回给前端
- 前端将 Token 存储在 `localStorage`，每次请求通过 `Authorization: Bearer <token>` 携带
- 后端 `AuthInterceptor` 拦截所有 `/api/**` 请求（排除 `/api/auth/**`），解析 Token 获取用户信息
- 通过 `@RequireRole` 注解实现方法级角色权限控制
- 未登录用户可访问公开页面（电影首页、详情），受限操作引导登录

### 6.2 锁座与超时释放

- 用户下单时，订单状态为 `待支付`，所选座位被锁定
- 订单记录 `lock_expire_time`（下单时间 + 10 分钟）
- 后端定时任务定期扫描超时订单，自动释放座位并更新订单状态

### 6.3 排期冲突检测

- 新增或编辑排期时，查询同一影厅的时间段是否重叠
- SQL 条件：`(start_time < #{endTime} AND end_time > #{startTime})`
- 若存在重叠，返回错误提示，阻止保存

### 6.4 冗余缓存策略

- 电影表的 `total_box_office`、`rating_avg`、`rating_count` 字段为冗余缓存
- 用户评分或订单完成后，实时更新这些字段
- 避免每次查询都进行聚合计算，提升查询性能

### 6.5 真实电影海报

- 电影海报 URL 通过 TMDB API 获取，格式为 `https://image.tmdb.org/t/p/w500/{poster_path}`
- 前端 `MoviePoster` 组件实现加载失败 fallback 机制，显示默认图片
- 数据库 `database.sql` 已包含 30 部电影的真实海报 URL

### 6.6 前端路由权限控制

```typescript
// 路由 meta 配置示例
{
  path: '/admin',
  meta: { requiresAuth: true, roles: ['ADMIN'] }
}
```

- `beforeEach` 导航守卫检查用户角色与路由要求的角色是否匹配
- 未登录用户访问需要认证的路由，自动跳转登录页并携带 redirect 参数
- 角色不匹配则跳转到对应角色首页
- 公开路由（`meta.public`）允许未登录访问

---

## 七、部署方案

| 组件 | 部署方式 | 说明 |
|------|----------|------|
| 前端 | Vercel | 已配置 `vercel.json`，支持 SPA 路由回退 |
| 后端 | 云服务器 | Spring Boot JAR 包部署，需公网可访问 |
| 数据库 | 云数据库 | MySQL 8.0，注意配置外网访问 |

---

## 八、测试账号

| 角色 | 用户名 | 密码 | 说明 |
|------|--------|------|------|
| 超级管理员 | `admin` | `123456` | 系统最高权限 |
| 普通用户 | `zhangsan` | `123456` | 已有收藏和订单数据 |
| 普通用户 | `lisi` | `123456` | 基础用户 |
| 影院管理员 | `cinema1` | `123456` | 已审核通过，绑定万象城影城 |
| 影院管理员 | `cinema2` | `123456` | 待审核状态 |

---

## 九、项目总结

### 9.1 完成情况

本项目完整实现了一个电影购票系统的核心功能，包括：

- ✅ 三种角色的完整权限体系
- ✅ 电影浏览、搜索、详情展示（含真实海报）
- ✅ 8×8 在线选座购票（锁座/超时释放）
- ✅ 订单全生命周期管理
- ✅ 影院管理员审核工作流
- ✅ 排期管理与冲突检测
- ✅ ECharts 数据统计可视化
- ✅ 系统公告管理
- ✅ 收藏与评分系统
- ✅ 用户注册与账户管理

### 9.2 项目特色

1. **前后端完全分离**：后端提供标准 REST API，前端独立开发部署
2. **模块化架构**：后端按业务领域划分模块，前端按角色划分页面
3. **审核工作流**：影院入驻和场次放映均需审批，体现真实业务场景
4. **排期冲突检测**：保证同一影厅不会出现时间重叠的排期
5. **性能优化**：冗余缓存策略减少数据库压力，锁座机制保证数据一致性
6. **真实海报资源**：通过 TMDB API 获取真实电影海报，提升用户体验

### 9.3 可扩展方向

- 接入真实支付接口（微信支付 / 支付宝）
- 座位图灵活配置（支持非 8×8 布局）
- 用户评论与回复功能
- 电影推荐算法（基于用户行为）
- 消息通知系统（站内信 / 邮件 / 短信）

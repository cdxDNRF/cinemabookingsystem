# 电影购票系统 —— 新手小白完全解读

> 📖 **阅读指南**：本文档是对《项目汇报文档》的逐章解读，用最通俗的语言帮你理解每一部分在说什么。如果你没有任何编程基础，从这篇开始看就对了！

---

## 🤔 这篇文章是干什么的？

你手上有两份文档：
1. **《项目汇报文档》** —— 正式的技术汇报，适合答辩时展示
2. **《新手小白完全解读》**（就是你现在看的这篇）—— 帮你理解汇报文档里那些"天书"到底在说什么

建议你先读完这篇，再去读汇报文档，就会轻松很多！

---

## 第一章：项目概述 —— 这个项目到底做了什么？

### 🎯 一句话总结

> 我们做了一个**仿猫眼电影**的网站，用户可以在上面浏览电影、选座位、买票，影院管理员可以管理影厅和排片，超级管理员负责审核和管理全局。

### 📌 汇报文档里那些词是什么意思？

| 术语 | 大白话解释 |
|------|-----------|
| **前后端完全分离** | 网站分两部分：一部分负责"好看"（前端，你看到的界面），一部分负责"好用"（后端，处理数据）。两部分各自独立开发，通过网络通信 |
| **三种角色** | 就像猫眼上有普通用户、影院工作人员、平台管理员一样，我们的系统也分三种人，每种人能做的事不一样 |
| **8×8 选座** | 每个影厅有 8 排、每排 8 个座位，你在网页上可以点选想要的座位 |
| **锁座/超时释放** | 你选好座位后，系统会帮你"占座" 10 分钟。10 分钟内不付款，座位就自动释放给别人 |
| **排期冲突检测** | 影院管理员安排场次时，系统会自动检查同一个影厅在同一时间是否已经安排了其他电影，避免冲突 |
| **审核工作流** | 影院管理员想入驻？先提交申请，等超级管理员审批通过才能用 |
| **ECharts 数据统计** | 用一个叫 ECharts 的工具画各种图表（折线图、饼图等），让数据看起来更直观 |
| **预告片播放** | 电影详情页可以直接播放预告片视频 |
| **真实电影海报** | 电影海报不是随便找的图，而是从 TMDB（一个电影数据库）获取的真实海报 |

---

## 第二章：技术架构 —— 用了什么技术？

### 🏗️ 先理解"架构"是什么

想象你要开一家餐厅：
- **前端** = 餐厅的大厅和菜单（顾客看到的一切）
- **后端** = 餐厅的厨房（处理订单、做菜）
- **数据库** = 餐厅的仓库（存放食材，也就是数据）
- **API** = 服务员（在前端和后端之间传递信息）

```
顾客（你） ──看菜单──► 大厅（前端） ──服务员（API）──► 厨房（后端） ──取食材──► 仓库（数据库）
```

### 📋 技术选型表解读

| 汇报文档里写的 | 是什么 | 为什么用它 |
|--------------|--------|-----------|
| **Spring Boot** | Java 的一个"框架"，相当于厨房的标准化设备 | Java 生态最主流，企业都在用，学了对找工作有帮助 |
| **MyBatis** | 帮助 Java 程序和数据库对话的工具 | 比 Hibernate 更灵活，写 SQL 更自由 |
| **Vue 3** | 做网页界面的工具，目前国内最火的前端框架之一 | 好学、好用、性能好 |
| **TypeScript** | JavaScript 的"升级版"，加了类型检查 | 写代码不容易出错，大项目必备 |
| **Element Plus** | Vue 3 的 UI 组件库，提供现成的按钮、表格、弹窗等 | 不用自己从零画按钮和表格，省时间 |
| **Tailwind CSS** | 写网页样式的工具 | 用类名直接控制样式，开发速度快 |
| **Pinia** | Vue 3 的状态管理工具 | 管理用户登录状态等全局数据 |
| **ECharts** | 百度出的图表工具 | 画折线图、饼图、柱状图特别方便 |
| **MySQL** | 数据库，用来存所有数据 | 最流行的关系型数据库，免费好用 |
| **JWT** | 一种身份验证方式 | 登录后发一张"通行证"，之后每次请求都带着它证明自己的身份 |
| **Vite** | 前端构建工具 | 启动快、打包快，开发体验好 |
| **Maven** | Java 项目的构建工具 | 自动管理依赖（第三方库），标准化构建流程 |

### 🔢 版本号是什么意思？

比如 `Spring Boot 3.3.5`：
- `3` = 大版本（有重大变化）
- `3` = 中版本（新增功能）
- `5` = 小版本（修 bug）

一般来说，大版本一样就能兼容。

---

## 第三章：系统设计 —— 系统是怎么设计的？

### 👥 三种角色详解

想象一个真实的电影院：

**1. 普通用户（你和我）**
- 🎬 浏览有什么电影在上映
- 💺 选座位、买票
- ⭐ 给电影打分、收藏
- 📋 查看自己的订单
- 📝 注册新账号、注销自己的账号

**2. 影院管理员（影院的工作人员）**
- 🏢 管理自己影院的信息（名字、地址、电话）
- 🎥 管理影厅（比如 1 号厅、2 号厅各有多少座位）
- 📅 安排排片（哪天几点放哪部电影），系统会自动检查时间是否冲突
- 📊 查看自己影院的订单

**3. 超级管理员（平台运营方）**
- ✅ 审核想入驻的影院
- 🎞️ 管理所有电影信息
- 📅 审核所有影院提交的排片
- 📈 查看全平台数据统计
- 📢 发布系统公告
- 👤 管理所有账号

### 🗄️ 数据库设计 —— 14 张表是什么意思？

**"表"是什么？**

你可以把数据库的"表"想象成 **Excel 表格**。每一张表存储一类信息。

**14 张表分别存什么？**

| 表名（代码里的名字） | 存什么内容 | 通俗理解 |
|---------------------|-----------|---------|
| `t_admin` | 超级管理员的账号信息 | 平台老板的账号 |
| `t_user` | 普通用户的账号信息 | 你的账号 |
| `t_cinema` | 影院信息 | 比如"万达影城"的名字、地址 |
| `t_cinema_admin` | 影院管理员的账号 + 审核状态 | 影院工作人员的账号 |
| `t_hall` | 影厅信息 | 比如"1号厅，8排8列" |
| `t_movie` | 电影信息 | 比如《流浪地球》的海报、简介、时长 |
| `t_movie_type` | 电影类型 | 比如"动作"、"科幻"、"喜剧" |
| `t_movie_type_rel` | 电影和类型的关联 | 《流浪地球》= 科幻 + 动作 |
| `t_movie_staff` | 演职人员 | 导演是谁、主演是谁 |
| `t_showing` | 放映场次 | 4月20日 14:00 在1号厅放《流浪地球》 |
| `t_order` | 订单 | 你买了一张票的记录 |
| `t_order_seat` | 订单里的具体座位 | 你买的是第3排第5座 |
| `t_notice` | 系统公告 | "系统维护通知"之类的 |
| `t_movie_user_action` | 用户的收藏和评分 | 你收藏了某部电影、打了8分 |

**"外键"是什么？**

外键就是表和表之间的"关联关系"。比如：
- `t_order`（订单表）里有一个 `user_id` 字段，指向 `t_user`（用户表）的 `id`
- 这就表示"这个订单属于哪个用户"

就像 Excel 里用 VLOOKUP 关联两个表格一样。

**"冗余缓存"是什么？**

正常来说，电影的平均评分应该每次都从所有评分里重新计算。但这样太慢了。

所以我们在 `t_movie`（电影表）里直接存了一个 `rating_avg`（平均评分）字段。每次有人打分，就更新这个字段。下次查询直接读取，不用重新计算。

就像你提前算好了班级平均分写在黑板上，不用每次都重新算一遍。

### 🔄 核心业务流程

#### 选座购票流程（最重要！）

```
第1步：你打开网站，看到正在上映的电影列表
         ⬇
第2步：你点进一部电影，看到详情和可选的场次
         ⬇
第3步：你选了一个场次，看到一个 8×8 的座位图
       （灰色=已售，白色=可选，蓝色=你选的）
         ⬇
第4步：你选了 2 个座位，点击"提交订单"
       → 系统创建订单，座位被"锁定"10分钟
       → 其他人看到这2个座位显示"已售"
         ⬇
第5步（分支A）：你在10分钟内付了款
       → 订单状态变为"待取票"
       → 你到影院取票，状态变为"已完成"
         ⬇
第5步（分支B）：你10分钟内没付款
       → 系统自动取消订单
       → 座位重新变为可选
```

#### 排期管理流程（影院管理员）

```
影院管理员登录后台
         ⬇
进入"排片管理"页面
         ⬇
选择电影、影厅、开始时间、结束时间、票价
         ⬇
点击"保存"
         ⬇
系统自动检查：这个影厅在这个时间段是不是已经有排期了？
         ⬇
   ┌─────────────┴─────────────┐
   ▼                           ▼
 没有冲突                    有冲突
   │                           │
   ▼                           ▼
 保存成功，等待审核          提示错误：该时间段已有排期
   │
   ▼
 超级管理员审核通过后，用户才能买票
```

#### 审核流程

```
影院管理员注册
    ⬇
提交影院信息（名字、地址等）
    ⬇
状态变为"待审核"（此时不能用）
    ⬇
超级管理员看到申请，点击"通过"或"驳回"
    ⬇
通过 → 绑定影院，可以正常使用
驳回 → 告知原因，可以修改后重新申请
```

---

## 第四章：功能模块 —— 网站上有哪些页面？

### 📱 前端页面一览

系统一共有 **19 个页面**，按角色分成了三组：

#### 你作为普通用户能看到的（5个页面）

| 页面 | 你能做什么 |
|------|-----------|
| 用户首页 | 看到热门电影推荐、排行榜、今日票房 |
| 电影列表 | 搜索电影、按类型/年份/地区筛选 |
| 电影详情 | 看海报、简介、预告片，收藏或评分 |
| 选座购票 | 看座位图、选座位、下单、付款 |
| 我的订单 | 看自己买过哪些票，可以取消没付的订单 |

#### 影院管理员能看到的（5个页面）

| 页面 | 能做什么 |
|------|---------|
| 员工首页 | 工作台概览 |
| 影院管理 | 修改影院信息 |
| 影厅管理 | 增删改影厅 |
| 排片管理 | 安排电影场次，系统会自动检测时间冲突 |
| 订单管理 | 看自己影院的订单 |

#### 超级管理员能看到的（8个页面）

| 页面 | 能做什么 |
|------|---------|
| 管理首页 | 后台概览 |
| 数据统计 | 看各种图表（票房趋势、类型分布等） |
| 影院审核 | 审核想入驻的影院 |
| 电影管理 | 增删改电影、管理类型和演职人员 |
| 排片管理 | 审核所有影院提交的排片 |
| 订单管理 | 看所有订单 |
| 公告管理 | 发布和管理公告 |
| 账号管理 | 管理所有用户账号 |

### 🔌 后端 API 是什么？

**API**（Application Programming Interface）就是前端和后端通信的"接口"。

你可以把 API 想象成**餐厅的菜单**：
- 前端（顾客）看菜单，点一道菜
- 后端（厨房）根据菜单做菜，把结果端出来

比如：
- 前端请求 `GET /api/movies` → 后端返回电影列表
- 前端请求 `POST /api/user/orders` → 后端创建一个新订单

**RESTful** 是一种 API 设计风格，简单说就是：
- 用 `GET` 表示"获取数据"
- 用 `POST` 表示"提交数据"
- 用 `DELETE` 表示"删除数据"
- URL 里用名词（`/movies`），不用动词（`/getMovies`）

---

## 第五章：项目结构 —— 代码是怎么组织的？

### 📁 后端代码结构

```
cinema-backend/
├── auth/          ← 登录、注册相关代码
├── common/        ← 公共工具（统一响应格式、权限检查、异常处理）
├── config/        ← 配置（跨域设置、拦截器注册）
└── module/        ← 业务代码（按功能模块分文件夹）
    ├── movie/     ← 电影相关的所有代码
    ├── order/     ← 订单相关的所有代码
    ├── cinema/    ← 影院相关的所有代码
    ├── showing/   ← 排期相关的所有代码
    └── ...        ← 其他模块
```

**为什么要这样分？**

就像你整理衣柜一样：上衣放一个抽屉，裤子放一个抽屉，袜子放一个抽屉。找东西的时候一目了然。

每个 `module` 文件夹里通常包含：
- `controller` — 接收前端请求的"入口"
- `service` — 处理业务逻辑的"大脑"
- `mapper` — 和数据库打交道的"手"
- `entity` — 数据的"形状"定义

### 📁 前端代码结构

```
cinema-frontend/
└── src/
    ├── api/          ← 封装和后端通信的代码
    ├── pages/        ← 所有页面
    │   ├── user/     ← 普通用户的页面
    │   ├── staff/    ← 影院管理员的页面
    │   └── admin/    ← 超级管理员的页面
    ├── router/       ← 路由（网址和页面的对应关系）
    ├── stores/       ← 状态管理（记住用户是否登录等）
    └── layouts/      ← 页面布局（侧边栏、顶栏等公共部分）
```

---

## 第六章：关键技术实现 —— 几个重要的技术点

### 🔐 JWT 认证（登录状态怎么保持的？）

**问题**：你在网页上登录后，刷新页面为什么不用重新登录？

**答案**：JWT（JSON Web Token）

流程：
```
1. 你输入用户名密码，点击登录
2. 后端验证通过，生成一串加密字符串（Token），返回给你
3. 前端把 Token 存在浏览器里（localStorage）
4. 之后你每次操作，前端都自动带上这个 Token
5. 后端看到 Token，就知道你是谁、有什么权限
```

就像你进入小区后拿到一张"门禁卡"，之后每次进出都刷卡，不用每次都登记。

### 💺 锁座机制（怎么防止两个人选同一个座位？）

**问题**：如果两个人同时选了同一个座位怎么办？

**答案**：下单即锁定

```
用户A选了3排5座 → 下单 → 座位状态变为"已锁定"
此时用户B看到3排5座显示"已售"，无法选择

如果用户A 10分钟内没付款 → 系统自动取消订单 → 座位重新可选
```

### 📅 排期冲突检测（怎么防止同一个影厅时间重叠？）

**问题**：如果影院管理员不小心把两部电影安排在同一时间同一个厅怎么办？

**答案**：系统自动检查

```
管理员提交排期：4月20日 14:00-16:00，1号厅，《流浪地球》
         ⬇
系统查询：1号厅在 14:00-16:00 之间有没有其他排期？
         ⬇
   ┌─────┴─────┐
   ▼           ▼
 没有          有
   │           │
   ▼           ▼
 保存成功     提示错误：该时间段已有排期
```

### 📊 冗余缓存（为什么要存重复数据？）

**问题**：电影评分明明可以从所有评分里算出来，为什么要单独存一个"平均评分"字段？

**答案**：性能！

- 不缓存：每次有人打开电影页面，都要从几万条评分记录里计算平均值 → **慢**
- 缓存：直接读取一个字段 → **快**

代价是每次有人打分时要更新这个字段，但打分的频率远低于查看的频率，所以划算。

### 🛡️ 权限控制（怎么防止普通用户访问管理后台？）

**两层防护**：

1. **前端路由守卫**：你在浏览器里直接输入管理员页面的网址 → 前端检查你的角色 → 不是管理员就跳转回首页
2. **后端权限注解**：就算绕过前端直接请求后端 API → 后端检查 Token 里的角色 → 不是管理员就拒绝

就像银行有两道门：前台检查你的预约，柜台还要验证你的身份证。

### 🖼️ 真实电影海报（海报是怎么来的？）

**问题**：电影海报是从哪里获取的？

**答案**：TMDB（The Movie Database）

- TMDB 是一个免费的公开电影数据库，里面有大量电影的海报、剧照等信息
- 我们的系统从 TMDB 获取海报的 URL 地址，直接显示在网页上
- 如果某个海报加载失败（比如网络问题），会显示一张默认的备用图片

---

## 第七章：部署方案 —— 怎么让网站上线？

### 🌐 部署是什么？

你在自己电脑上运行的项目，只有你自己能访问。**部署**就是把项目放到服务器上，让所有人都能通过网址访问。

### 📋 部署方案

| 组件 | 部署到哪里 | 说明 |
|------|-----------|------|
| 前端 | Vercel | 一个免费的网站托管平台，特别适合 Vue/React 项目 |
| 后端 | 云服务器 | 比如阿里云、腾讯云，需要花钱租 |
| 数据库 | 云数据库 | 也可以装在云服务器上 |

**Vercel** 是什么？你可以理解为"前端项目的免费酒店"——你把代码传上去，它自动帮你部署好，还给你一个网址。

---

## 第八章：测试账号 —— 怎么登录体验？

汇报文档里列出了 5 个测试账号，你可以用它们登录体验不同角色的功能：

| 你想体验什么 | 用哪个账号 |
|-------------|-----------|
| 买票、看电影 | 用户名 `zhangsan`，密码 `123456` |
| 管理影厅、排片 | 用户名 `cinema1`，密码 `123456` |
| 审核管理、看统计 | 用户名 `admin`，密码 `123456` |

---

## 第九章：项目总结 —— 这个项目做得怎么样？

### ✅ 做了什么

一个完整的电影购票系统，从浏览电影到买票取票，从影院管理到平台运营，全流程都实现了。

### 🌟 有什么亮点

1. **三种角色分工明确** —— 不是简单的增删改查，而是有真实的业务逻辑
2. **排期冲突检测** —— 自动防止同一影厅时间重叠，避免人工失误
3. **锁座机制** —— 解决了"并发选座"这个实际问题
4. **审核工作流** —— 模拟了真实世界中的审批流程
5. **数据可视化** —— 图表让数据一目了然
6. **真实电影海报** —— 从 TMDB 获取，提升真实感

### 📊 代码量

整个项目大约 62% 是 Java 后端代码，37% 是 Vue 前端代码，1% 是配置和 SQL。

---

## ❓ 常见问题

### Q：答辩时老师可能会问什么？

**Q1：为什么选择前后端分离？**
> A：前后端分离可以让前端和后端独立开发、独立部署。前端专注于界面交互，后端专注于业务逻辑和数据。这也是目前企业级开发的主流方式。

**Q2：JWT 和 Session 有什么区别？**
> A：Session 是服务器端存储用户状态，JWT 是客户端存储。JWT 更适合前后端分离架构，因为不需要服务器维护 Session 状态。

**Q3：如果两个用户同时选同一个座位怎么办？**
> A：我们使用数据库的事务和唯一约束来保证。下单时会检查座位是否已被占用，如果已被占用则返回错误。

**Q4：排期冲突是怎么检测的？**
> A：使用 SQL 查询检查同一影厅的时间段是否有重叠。条件是：新排期的开始时间 < 已有排期的结束时间，且新排期的结束时间 > 已有排期的开始时间。

**Q5：为什么电影表要冗余存储评分和票房？**
> A：这是一种"空间换时间"的策略。评分和票房是高频查询字段，冗余存储可以避免每次都做聚合计算，提升查询性能。

**Q6：电影海报是从哪里来的？**
> A：从 TMDB（The Movie Database）获取的真实海报 URL。如果加载失败，会显示默认的备用图片。

**Q7：项目有什么不足之处？**
> A：（你可以根据实际情况回答）比如可以加入支付接口对接、座位图可以做得更灵活（不是固定 8×8）、可以加入用户评论回复功能等。

---

## 📚 术语速查表

| 术语 | 英文 | 一句话解释 |
|------|------|-----------|
| 前端 | Frontend | 用户看到的网页界面 |
| 后端 | Backend | 服务器端处理逻辑的程序 |
| 数据库 | Database | 存储数据的系统 |
| API | Application Programming Interface | 前后端通信的接口 |
| 框架 | Framework | 提供基础功能的代码库，让你不用从零开始 |
| 组件库 | Component Library | 提供现成 UI 组件（按钮、表格等）的库 |
| 路由 | Router | 网址和页面的对应关系 |
| 状态管理 | State Management | 管理全局数据（如登录状态）的方式 |
| Token | Token | 身份验证的通行证 |
| CRUD | Create, Read, Update, Delete | 增删改查 |
| 部署 | Deploy | 把项目放到服务器上让所有人访问 |
| 实体类 | Entity | 对应数据库表的 Java 类 |
| 控制器 | Controller | 接收请求、返回响应的代码 |
| 拦截器 | Interceptor | 在请求到达控制器之前进行处理的代码 |
| 外键 | Foreign Key | 表和表之间的关联关系 |
| 冗余 | Redundancy | 存储重复数据以提升性能 |
| 事务 | Transaction | 保证多个操作要么全部成功，要么全部失败 |
| SPA | Single Page Application | 单页应用，页面不刷新，内容动态更新 |
| RESTful | RESTful | 一种 API 设计风格 |
| ORM | Object-Relational Mapping | 对象和数据库表之间的映射工具 |
| TMDB | The Movie Database | 一个免费的公开电影数据库 |

---

> 💡 **最后的小建议**：答辩时不用把所有技术细节都说出来。重点讲清楚 **你做了什么、为什么这么做、有什么亮点** 就够了。如果老师追问技术细节，再深入回答。
>
> 加油！🎉

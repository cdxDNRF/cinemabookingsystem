# 电影购票系统启动指南

## 环境要求

- MySQL 8.0+（服务名通常为 MySQL80）
- Maven 3.8+
- Node.js 18+

## 启动步骤

### 1. 启动 MySQL

**方式一：服务管理器**
1. `Win + R` 输入 `services.msc` 回车
2. 找到 `MySQL80` 服务
3. 右键 → 启动

**方式二：PowerShell（管理员）**
```powershell
Start-Service MySQL80
```

### 2. 启动后端

```powershell
cd cinema-backend
mvn spring-boot:run
```

等待出现 `Started CinemaBackendApplication` 即启动成功。

### 3. 启动前端

**新开一个 PowerShell 窗口：**

```powershell
cd cinema-frontend
npm run dev
```

等待出现 `VITE v5.x ready` 即启动成功。

### 4. 访问系统

浏览器打开 http://localhost:5173

## 测试账号

| 角色 | 账号 | 密码 |
|------|------|------|
| 超级管理员 | admin | 123456 |
| 普通用户 | zhangsan | 123456 |
| 影院管理员 | cinema1 | 123456 |

## 停止服务

### 停止前后端

关闭前端和后端的 PowerShell 窗口即可。

### 停止 MySQL

**方式一：服务管理器**
1. `Win + R` 输入 `services.msc` 回车
2. 找到 `MySQL80` 服务
3. 右键 → 停止

**方式二：PowerShell（管理员）**
```powershell
Stop-Service MySQL80
```
